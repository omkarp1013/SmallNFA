#include <iostream>
#include <vector>
using namespace std;


int main() {
    int n;
    cin >> n;

    int* p = primes(n);

    
}

int* primes(int n) {
    // Finds primes p1, p2, ..., pl
    vector<int> res;
}

bool is_prime(int n) {
    // Tests if a number is prime
    (for int i = 0; i < )
}

